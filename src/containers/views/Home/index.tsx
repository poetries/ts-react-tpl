import * as React from 'react'

import {ComponentExt} from '@utils/reactExt'

class Home extends ComponentExt {
    render() {
        return (
            <div>Home</div>
        )
    }
}

export default Home