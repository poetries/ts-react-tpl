import * as React from 'react'

import {ComponentExt} from '@utils/reactExt'

class Page extends ComponentExt {
    render() {
        return (
            <div>Home</div>
        )
    }
}

export default Page